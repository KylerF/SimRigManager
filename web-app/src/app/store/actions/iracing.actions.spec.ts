import * as IracingActions from './iracing.actions';

describe('iRacing data update action', () => {
  it('should create an instance', () => {
    expect(new IracingActions.UpdateIracing()).toBeTruthy();
  });
});
